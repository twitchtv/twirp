name: commit checks
on:
  push:
jobs:
  run:
    name: tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ['^1.16.0', '^1.15.0', '^1.14.0']
    env:
      GO111MODULE: 'off'
      GOPATH: '${{ github.workspace }}/go'
    steps:
    - uses: actions/checkout@v2
      with:
        path: go/src/github.com/twitchtv/
    - name: Install Go
      uses: actions/setup-go@v2
      with:
        go-version: ${{ matrix.go-version }}
    - name: Run Go Tests
      run: |
        cd go/src/github.com/twitchtv/twirp
        make test
    - name: Show stuff in console
      run: |
        cd go/src/github.com/twitchtv/twirp
        ls -lisa
        pwd
        cat Makefile
    - name: Run clientcompat Tests
      run: |
        cd go/src/github.com/twitchtv/twirp
        make test_clientcompat
